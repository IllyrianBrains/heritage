# IB-Heritage

MkDocs Material site for Albanian culture, language, memory, and open knowledge (OSM/Wikipedia).

## Local run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
mkdocs serve
```
